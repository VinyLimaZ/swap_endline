<%= form_with(model: swap, local: true) do |form| %>
  <% if swap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(swap.errors.count, "error") %> prohibited this swap from being saved:</h2>

      <ul>
        <% swap.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
